services:
  frontend:
    build:
      context: ./frontend
    container_name: cryptocactus-frontend
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
    networks:
      - cryptocactus_network

networks:
  cryptocactus_network:
    driver: bridge
